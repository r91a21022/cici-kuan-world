/* ============================================================
   網站設定（爸爸專用，改這一個檔就好）
   這裡的東西不影響孩子看到的內容，是給你調整功能用的。
   ============================================================ */
window.SITE_CONFIG = {

  /* ---- 進站密碼 ----
     想給網站加一層簡單密碼（避免陌生人點進來），
     把 gatePassword 換成你想要的密碼。留空字串 "" 就不設密碼。
     提醒：這是簡單的門簾，不是保險箱。真正要防護，之後我教你用主機的密碼保護。 */
  gatePassword: "",

  /* ---- 訪客計數器 ----
     兩種模式都不會讓網站壞掉：
     1) 還沒設定 Supabase：計數器會顯示本機造訪次數（你自己這台看得到）。
     2) 設定好 Supabase：全家跨裝置共用同一個真實數字。
     設定方法我會另外用白話一步一步教你，屆時把下面兩個值貼上就好。 */
  supabaseUrl: "",       // 例如 https://xxxxx.supabase.co
  supabaseAnonKey: "",   // 那一長串 anon public key

  /* 計數器起始基數（想讓數字看起來溫暖一點可以設，例如 first family visits） */
  counterBase: 0
};
