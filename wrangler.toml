name = "cih-kuan-world"
main = "worker.js"
compatibility_date = "2025-01-01"

# 純靜態網站，把整個資料夾當作網頁直接服務。
# worker.js 會在每個請求前先跑，檢查家人密碼；沒設密碼時照常開放。
[assets]
directory = "."
binding = "ASSETS"
run_worker_first = true
